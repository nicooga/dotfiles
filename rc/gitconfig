[core]
	excludesfile = /home/cygnus/.gitignore
	pager = most +s +'/---'
	editor = gvim --nofork

[color]
	diff = auto
	branch = auto
	status = auto
	ui = auto
	log = auto

[merge]
	tool = meld

[alias]
	up = pull origin
	st = status -sb
	di = diff
	co = checkout
	ci = commit
	br = branch
	sta = stash
	lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr)%Creset' --abbrev-commit --date=relative
	addp = add -p
	addi = add -i

[user]
	name = Nicolas Oga
	email = 2112.oga@gmail.com

[diff "ruby"]
	funcname = ^ *\\(\\(class\\|module\\|def\\) .*\\)

[github]
	user = NicolasOga
	token = FILLUP

[diff]
	tool = meld
[credential]
	helper = cache --timeout=3600
